============
Installation
============

At the command line::

    $ easy_install python-viscosity-app

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-viscosity-app
    $ pip install python-viscosity-app